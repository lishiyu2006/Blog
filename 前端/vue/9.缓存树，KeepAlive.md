# 9.缓存树，KeepAlive

KeepAlive 会将内部的component缓存起来不会像v-if一样卸载，而是达到类似v-show的样子，当内部组件切换的时候，不会重复访问调用接口

~~~vue
  <KeepAlive>  
    <component :is="currentComponent"></component>  
  </KeepAlive>
~~~

## 生命周期函数

actived和deactiva