# component组件运行后渲染问题

## 前提

假设底层有一个组件从App.vue里面的component组件props到了一个数组

而且这个数组在底层组件接收的时候没有设置default,并且这个响应式
参数没有在底层的组件的HTML里面使用,他就会懒加载,当要使用的时候,又因为在底层组件的函数执行是比props找数组要快,就会导致如果底层组件的函数比如用了some,但是这个Array是NULL,就会报错

## 解决方法

1. 在底层组件加上默认值
2. 在别的