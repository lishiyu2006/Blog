路线：html->css->js->vue(这里跳过ts是因为有比赛比较着急会写项目)



项目大纲
~~~bash
src/(Source源目录)
- assets/ (静态资源)
  - main.css(主要样式)
- components/(组件)
- router/(路由):配置页面的跳转规则
- utils/(工具类)
- views/(页面)
App.vue(根组件)
main.js(入口文件):整个项目的启动脚本
node—model/(库根目录)：根据你的package.json文件自动下载生成的
~~~