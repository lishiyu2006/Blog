# API

本质：是通过function函数接受json数据
1. 在接受数据的函数开始有`async` 是为了处理**异步操作**
		例如：点击刷新列表，要重新等待后端将数据库的数据通过api传入下课显示，所以要async等待数据接收到,避免界面等待数据传输而卡住
2. fetch 和 axios 的 区别
		体积上:fetch,相较于axios,更加轻量级,而且是原生的,axios是需要pnpm下载的库
		功能上:fetch只提供基础的抓取,没有封装其他功能,而且数据需要手动转json
